{
  "name": "axiom-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npx concurrently \"npm run dev --workspace=frontend\" \"cd backend && python -m uvicorn main:app --reload\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "next-auth": "^4.24.14"
  }
}